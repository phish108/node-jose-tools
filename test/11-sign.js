/* eslint-env node, mocha */
/* eslint-disable require-jsdoc */

const chai = require("chai");
const expect = chai.expect;

describe( "sign tool tests", function() {
    it("basic RSA signing", async () => {});
    it("basic RSA signing bad kid", async () => {});
    it("basic RSA signing with header kid", async () => {});
    it("basic RSA signing with header kid, non exsting kid", async () => {});
    it("basic RSA signing bad kid", async () => {});
    it("basic RSA signing with extended headers", async () => {});
    it("basic RSA signing compact format", async () => {});
    it("basic RSA signing general format", async () => {});
    it("basic EC signing", async () => {});
    it("basic OCT signing", async () => {});
    it("basic OCT signing wrong key size", async () => {});
    it("check issuer time", async () => {});
    it("add token experiration", async () => {});
    it("add audience experiration", async () => {});
    it("add subject experiration", async () => {});
    it("add issuer experiration", async () => {});
} );
