/* eslint-env node, mocha */
/* eslint-disable require-jsdoc */

const chai = require("chai");
const expect = chai.expect;

describe( "encrypt tool tests", function() {
    it("encrypt for aud", async () => {});

    it("encrypt for aud with kid", async () => {});
    it("encrypt for aud with kid and alg", async () => {});
    it("encrypt with enc", async () => {});
    it("encrypt to general", async () => {});
    it("encrypt to flattened", async () => {});
    it("encrypt to compact", async () => {});

    it("encrypt without key", async () => {});
    it("encrypt with empty payload", async () => {});
} );
